/** @type {import('next').NextConfig} */
const nextConfig = {
    // comment on these lines before running locally
    // output: 'export',
    // basePath: '/_next',
    assetPrefix:'.',
}

module.exports = nextConfig
